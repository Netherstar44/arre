kind = "web"
previewPath = "/"
title = "GestureForge 3D"
version = "1.0.0"
id = "artifacts/gesture-forge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19478

[services.development]
run = "pnpm --filter @workspace/gesture-forge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gesture-forge", "run", "build" ]
publicDir = "artifacts/gesture-forge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19478"
BASE_PATH = "/"
