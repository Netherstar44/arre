{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json', 'yarn.lock'].forEach(f => fs.existsSync(f) && fs.unlinkSync(f)); if (!/pnpm/.test(process.env.npm_config_user_agent || '')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "pnpm -r --parallel --filter @workspace/api-server --filter @workspace/gesture-forge run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build && node -e \"const fs=require('fs'); if(fs.existsSync('artifacts/gesture-forge/dist')){fs.cpSync('artifacts/gesture-forge/dist', 'public', {recursive:true});}\"",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "express": "^5.2.1",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
